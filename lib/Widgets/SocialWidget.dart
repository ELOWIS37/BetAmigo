import 'package:flutter/material.dart';

class SocialWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Social y Amigos'),
      ),
      body: Center(
        child: Text('Pantalla de Social'),
      ),
    );
  }
}
